struct duom {
    string vardas;
    string pavarde;
    double vid;
    double med;
    int egz;
};

duom ivedimas(int &n);

duom generavimas(int &n);

duom nuskaitymas();

void isvedimasVid(duom temp, int n);

void isvedimasMed(duom temp, int n);
