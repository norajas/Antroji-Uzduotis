struct duom {
    string vardas;
    string pavarde;
    double vid;
    int egz;
    double gal;
};

void gen(int x);
vector<duom> nusk(int x);
string pazGeneravimas();
void isv(vector<duom> v);
